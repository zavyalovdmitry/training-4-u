const initialState = {
   nameProject: "Тренажер для детей"
};

const headerProjecrReducer = (state = initialState, action) => {
   switch (action.type) {
      default:
         return state
   }
};
export default headerProjecrReducer;